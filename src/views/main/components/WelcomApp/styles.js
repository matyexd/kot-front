import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  title: {
    fontSize: 36,
    fontWeight: 700,
  },
  textWelcome: {
    fontSize: 18,
    fontWeight: 500,
    marginTop: 20,
  },
  imgBox: {
    width: 70,
    height: 70,
    display: "flex",
    alignItems: "center",
  },
}));
